galera = []
pessoa=dict()
soma=media=0
while True:
    pessoa.clear()
    pessoa['nome']=str(input('nome: '))
    pessoa['idade']=int(input('idade: '))
    soma+=pessoa['idade']
    while True:
        pessoa['sexo']=str(input('sexo: M/F: ')).upper()[0]
        if pessoa['sexo'] in 'MFee':
            break
        print('digite um sexo de verdade!')
    galera.append(pessoa.copy())
    while True:
        resp=str(input('deseja continuar? S/N: ')).upper()[0]
        if resp in 'SN':
            break
    if resp =='N':
        break
print(galera)
print(f'a quantidade de cadastrados é {len(galera)}')
media=soma/len(galera)
print(f'a soma é {soma} e a media {media}')
print('o nome das mulheres é: ',end='')
for p in galera:
    if p['sexo'] == 'F':
        print(f'{p['nome']} ',end='')
print()
print('os maiores que a media são: ',end='')
for c in galera:
    if c['idade']>media:
        print(f'{c['nome']}', end='')